rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Dashboard personal — solo lectura/escritura server-side via Admin SDK
    // Bloquear acceso directo desde el cliente
    match /{document=**} {
      allow read, write: if false;
    }
  }
}
